package com.pwc.query.controllers.models.factories;

public class ContentsFactory {

   /* public ControllerContent getCollectionContent(){

        switch (ControllerType.valueOf(query.toUpperCase())) {
            case ADVCOLLECTION:
                filterQuery = new CollectionContent();
                break;
            case FACETED:
                filterQuery = new FacetedContent();
                break;
            default:
                throw new IllegalArgumentException("Invalid filter " + query);
        }

        return filterQuery;
    }*/

}
